{
  "name": "enthropic-trading-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise-grade trading platform with authentication",
  "scripts": {
    "setup": "./tools/scripts/setup-dev.sh",
    "start": "./tools/scripts/run-all.sh",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "proto:generate": "./tools/scripts/generate-protos.sh"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "nx": "^17.2.0"
  },
  "workspaces": [
    "apps/*",
    "libs/shared/*"
  ],
  "dependencies": {
    "@nestjs/jwt": "^11.0.2",
    "@nestjs/passport": "^11.0.5",
    "bcrypt": "^6.0.0",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1"
  }
}
