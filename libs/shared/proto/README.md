# Protocol Buffers

This directory contains shared Protocol Buffer definitions.

## Generating Code

Run the generate script:

```bash
./generate.sh
```

This will generate:
- TypeScript types for NestJS services
- Rust types for execution-core
- Python types for strategy-service
